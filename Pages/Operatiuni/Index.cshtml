@page
@model WebApplication6.Pages.Operatiuni.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Operatiune[0].Angajat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Operatiune[0].AngajatID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Operatiune[0].ReturnDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Operatiune[0].Client.Autovehicul)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Operatiune) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Angajat.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AngajatID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReturnDate)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Client.Autovehicul.Model)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
